# FlySend WA

## Project overview

FlySend WA is a client-side template manager for preparing and sending repeated WhatsApp messages.

## User preferences

- Template data must remain local to the user's browser. Use browser `localStorage` only; do not add server APIs, external databases, analytics, or other remote persistence for templates.